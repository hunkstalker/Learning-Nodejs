#Notas:

```
Opciones:
      --help     Muestra ayuda
      --version  Muestra número de versión
  -b, --base     Base de la tabla
  -s, --show     Mostrar tabla
  -l, --limit    Limite de la tabla
```
